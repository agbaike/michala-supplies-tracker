/* Example Firebase configuration.
   Copy this file to firebase.config.js and fill in your own
   Firebase Realtime Database URL. firebase.config.js is gitignored
   so your real database link is never committed to source control. */
const FIREBASE_DB_URL = 'https://YOUR-PROJECT-default-rtdb.YOUR-REGION.firebasedatabase.app';
const DB_PATH = 'inventory';
